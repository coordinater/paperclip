{
  "name": "@ai-company/paperclip-plugin-codemod",
  "version": "0.0.1-alpha.0",
  "private": true,
  "type": "module",
  "description": "Paperclip plugin · run jscodeshift / codemod transforms via webhook. M2 W6-D3 (JS 栈选择路径).",
  "keywords": ["paperclip", "plugin", "codemod", "jscodeshift", "ai-company"],
  "exports": {
    ".": "./src/worker.ts",
    "./manifest": "./src/manifest.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9",
    "vitest": "^4.1.8"
  }
}
